<div class="split-sidebar fixed-w-250 h-100 bg-white p-3">
  <table class="table table-striped">
    <thead>
      <tr>
        <th>コード</th>
        <th>職名</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>X00</td>
        <td><%= link_to "新規作成", new_job_path, {class: ""} %></td>
      </tr>
      <% jobs.each do |job| %>
        <tr>
          <td><%= job.code %></td>
          <td><%= job.name %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>